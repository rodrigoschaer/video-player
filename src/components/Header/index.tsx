import { IgniteLabLogo } from "../../assets/IgniteLabLogo";

export const Header = () => {
  return (
    <header className="w-full py-5 flex items-center justify-center">
      <IgniteLabLogo />
    </header>
  );
};
